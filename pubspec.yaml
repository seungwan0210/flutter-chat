name: dartschat
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.10.1
  firebase_auth: ^5.4.2
  cloud_firestore: ^5.6.3
  image_picker: ^1.1.2
  firebase_storage: ^12.4.2
  firebase_messaging: ^15.2.2
  google_fonts: ^6.1.0  # 커스텀 폰트 지원
  url_launcher: ^6.1.14
  intl: ^0.19.0
  logger: ^2.0.2  # 디버깅 개선

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/logo.jpg
    - assets/default_profile.png
    - assets/bulls_fighter.webp
    - assets/dartslive.webp
    - assets/phoenix.webp
  uses-material-design: true

  # 커스텀 폰트 예시 (선택 사항)
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Bold.ttf
  #         weight: 700